import React from 'react'
import Header from '../component/Header/Header'
import Nav from '../component/Nav/Nav'

const Home = ( ) => {
    return(
        <div>
            <Nav />
            <Header />
        </div>
    )
}

export default Home